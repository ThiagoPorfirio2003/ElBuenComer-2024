import { EstadoPedidoPipe } from './estado-pedido.pipe';

describe('EstadoPedidoPipe', () => {
  it('create an instance', () => {
    const pipe = new EstadoPedidoPipe();
    expect(pipe).toBeTruthy();
  });
});
