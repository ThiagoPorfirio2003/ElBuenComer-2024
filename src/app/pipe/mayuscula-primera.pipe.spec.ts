import { MayusculaPrimeraPipe } from './mayuscula-primera.pipe';

describe('MayusculaPrimeraPipe', () => {
  it('create an instance', () => {
    const pipe = new MayusculaPrimeraPipe();
    expect(pipe).toBeTruthy();
  });
});
